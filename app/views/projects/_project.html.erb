<%= div_for project do%>
	<%= link_to project.name, project %>
	<%= link_to 'edit', edit_project_path(project) %>
	<%= link_to 'delete', project, method: :delete, remote: true, 
		data: {confirm: "Really delete the project?"} %>
<%end%>